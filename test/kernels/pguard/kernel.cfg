# pguard: a guarded compare writing a predicate other than its guard (Q-21).
# 32 threads, one warp. SOURCE with a core: prefix is relative to THIS repo;
# the compiler repo still assembles and runs it. No pokes.
SOURCE=core:test/kernels/pguard/kernel.s
