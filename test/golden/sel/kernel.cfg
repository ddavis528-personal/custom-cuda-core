# sel: a predicate read as data in the lane. 32 threads, one warp.
# SOURCE with a core: prefix is relative to THIS repo; the compiler repo
# still assembles and runs it. No pokes: the kernel reads no memory.
SOURCE=core:test/golden/sel/kernel.s
